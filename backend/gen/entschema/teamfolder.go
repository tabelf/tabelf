// Code generated by entc, DO NOT EDIT.

package entschema

import (
	"fmt"
	"strings"
	"tabelf/backend/gen/entschema/teamfolder"
	"time"

	"entgo.io/ent/dialect/sql"
)

// TeamFolder is the model entity for the TeamFolder schema.
type TeamFolder struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// UID holds the value of the "uid" field.
	UID string `json:"uid,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeactivatedAt holds the value of the "deactivated_at" field.
	DeactivatedAt *time.Time `json:"deactivated_at,omitempty"`
	// TeamUID holds the value of the "team_uid" field.
	// 团队uid
	TeamUID string `json:"team_uid,omitempty"`
	// FileName holds the value of the "file_name" field.
	// 文件名称
	FileName string `json:"file_name,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamFolder) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamfolder.FieldID:
			values[i] = new(sql.NullInt64)
		case teamfolder.FieldUID, teamfolder.FieldTeamUID, teamfolder.FieldFileName:
			values[i] = new(sql.NullString)
		case teamfolder.FieldCreatedAt, teamfolder.FieldUpdatedAt, teamfolder.FieldDeactivatedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type TeamFolder", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamFolder fields.
func (tf *TeamFolder) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamfolder.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tf.ID = uint64(value.Int64)
		case teamfolder.FieldUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field uid", values[i])
			} else if value.Valid {
				tf.UID = value.String
			}
		case teamfolder.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tf.CreatedAt = value.Time
			}
		case teamfolder.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tf.UpdatedAt = value.Time
			}
		case teamfolder.FieldDeactivatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deactivated_at", values[i])
			} else if value.Valid {
				tf.DeactivatedAt = new(time.Time)
				*tf.DeactivatedAt = value.Time
			}
		case teamfolder.FieldTeamUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field team_uid", values[i])
			} else if value.Valid {
				tf.TeamUID = value.String
			}
		case teamfolder.FieldFileName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field file_name", values[i])
			} else if value.Valid {
				tf.FileName = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this TeamFolder.
// Note that you need to call TeamFolder.Unwrap() before calling this method if this TeamFolder
// was returned from a transaction, and the transaction was committed or rolled back.
func (tf *TeamFolder) Update() *TeamFolderUpdateOne {
	return (&TeamFolderClient{config: tf.config}).UpdateOne(tf)
}

// Unwrap unwraps the TeamFolder entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tf *TeamFolder) Unwrap() *TeamFolder {
	tx, ok := tf.config.driver.(*txDriver)
	if !ok {
		panic("entschema: TeamFolder is not a transactional entity")
	}
	tf.config.driver = tx.drv
	return tf
}

// String implements the fmt.Stringer.
func (tf *TeamFolder) String() string {
	var builder strings.Builder
	builder.WriteString("TeamFolder(")
	builder.WriteString(fmt.Sprintf("id=%v", tf.ID))
	builder.WriteString(", uid=")
	builder.WriteString(tf.UID)
	builder.WriteString(", created_at=")
	builder.WriteString(tf.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(tf.UpdatedAt.Format(time.ANSIC))
	if v := tf.DeactivatedAt; v != nil {
		builder.WriteString(", deactivated_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", team_uid=")
	builder.WriteString(tf.TeamUID)
	builder.WriteString(", file_name=")
	builder.WriteString(tf.FileName)
	builder.WriteByte(')')
	return builder.String()
}

// TeamFolders is a parsable slice of TeamFolder.
type TeamFolders []*TeamFolder

func (tf TeamFolders) config(cfg config) {
	for _i := range tf {
		tf[_i].config = cfg
	}
}
