// Code generated by entc, DO NOT EDIT.

package entschema

import (
	"entgo.io/ent"
	"entgo.io/ent/dialect"
)

// Option function to configure the client.
type Option func(*config)

// Config is the configuration for the client and its builder.
type config struct {
	// driver used for executing database requests.
	driver dialect.Driver
	// debug enable a debug logging.
	debug bool
	// log used for logging on debug mode.
	log func(...interface{})
	// hooks to execute on mutations.
	hooks *hooks
}

// hooks per client, for fast access.
type hooks struct {
	Account             []ent.Hook
	Admin               []ent.Hook
	Collaboration       []ent.Hook
	Community           []ent.Hook
	CommunityCategory   []ent.Hook
	CommunityMeta       []ent.Hook
	Feedback            []ent.Hook
	Focus               []ent.Hook
	GoodArticle         []ent.Hook
	GoodArticleCategory []ent.Hook
	GoodArticleHot      []ent.Hook
	GoodArticleMeta     []ent.Hook
	Invite              []ent.Hook
	Message             []ent.Hook
	Notice              []ent.Hook
	PayOrder            []ent.Hook
	PayOrderFeedback    []ent.Hook
	PersonalFolder      []ent.Hook
	Recharge            []ent.Hook
	ShareLink           []ent.Hook
	Station             []ent.Hook
	StationCategory     []ent.Hook
	StationMeta         []ent.Hook
	Team                []ent.Hook
	TeamFolder          []ent.Hook
	TeamGroup           []ent.Hook
	UrlCrawl            []ent.Hook
	WebLink             []ent.Hook
	Workspace           []ent.Hook
}

// Options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...interface{})) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}
