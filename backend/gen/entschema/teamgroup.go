// Code generated by entc, DO NOT EDIT.

package entschema

import (
	"fmt"
	"strings"
	"tabelf/backend/gen/entschema/teamgroup"
	"time"

	"entgo.io/ent/dialect/sql"
)

// TeamGroup is the model entity for the TeamGroup schema.
type TeamGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// UID holds the value of the "uid" field.
	UID string `json:"uid,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeactivatedAt holds the value of the "deactivated_at" field.
	DeactivatedAt *time.Time `json:"deactivated_at,omitempty"`
	// UserUID holds the value of the "user_uid" field.
	// 用户uid
	UserUID string `json:"user_uid,omitempty"`
	// TeamUID holds the value of the "team_uid" field.
	// 团队uid
	TeamUID string `json:"team_uid,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeamGroup) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case teamgroup.FieldID:
			values[i] = new(sql.NullInt64)
		case teamgroup.FieldUID, teamgroup.FieldUserUID, teamgroup.FieldTeamUID:
			values[i] = new(sql.NullString)
		case teamgroup.FieldCreatedAt, teamgroup.FieldUpdatedAt, teamgroup.FieldDeactivatedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type TeamGroup", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeamGroup fields.
func (tg *TeamGroup) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teamgroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tg.ID = uint64(value.Int64)
		case teamgroup.FieldUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field uid", values[i])
			} else if value.Valid {
				tg.UID = value.String
			}
		case teamgroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tg.CreatedAt = value.Time
			}
		case teamgroup.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tg.UpdatedAt = value.Time
			}
		case teamgroup.FieldDeactivatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deactivated_at", values[i])
			} else if value.Valid {
				tg.DeactivatedAt = new(time.Time)
				*tg.DeactivatedAt = value.Time
			}
		case teamgroup.FieldUserUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_uid", values[i])
			} else if value.Valid {
				tg.UserUID = value.String
			}
		case teamgroup.FieldTeamUID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field team_uid", values[i])
			} else if value.Valid {
				tg.TeamUID = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this TeamGroup.
// Note that you need to call TeamGroup.Unwrap() before calling this method if this TeamGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (tg *TeamGroup) Update() *TeamGroupUpdateOne {
	return (&TeamGroupClient{config: tg.config}).UpdateOne(tg)
}

// Unwrap unwraps the TeamGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tg *TeamGroup) Unwrap() *TeamGroup {
	tx, ok := tg.config.driver.(*txDriver)
	if !ok {
		panic("entschema: TeamGroup is not a transactional entity")
	}
	tg.config.driver = tx.drv
	return tg
}

// String implements the fmt.Stringer.
func (tg *TeamGroup) String() string {
	var builder strings.Builder
	builder.WriteString("TeamGroup(")
	builder.WriteString(fmt.Sprintf("id=%v", tg.ID))
	builder.WriteString(", uid=")
	builder.WriteString(tg.UID)
	builder.WriteString(", created_at=")
	builder.WriteString(tg.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(tg.UpdatedAt.Format(time.ANSIC))
	if v := tg.DeactivatedAt; v != nil {
		builder.WriteString(", deactivated_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", user_uid=")
	builder.WriteString(tg.UserUID)
	builder.WriteString(", team_uid=")
	builder.WriteString(tg.TeamUID)
	builder.WriteByte(')')
	return builder.String()
}

// TeamGroups is a parsable slice of TeamGroup.
type TeamGroups []*TeamGroup

func (tg TeamGroups) config(cfg config) {
	for _i := range tg {
		tg[_i].config = cfg
	}
}
